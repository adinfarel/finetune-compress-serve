experiment: "vllm_awq"
output_dir: "results/stage3/vllm_awq"

model:
  awq_model_path: "results/stage2/quant_awq"

vllm:
  gpu_memory_utilization: 0.90
  max_model_len: 1024
  dtype: "float16"
  quantization: "awq"
  temperature: 0.0
  max_tokens: 128

benchmark:
  concurrency_levels: [1, 4, 8, 16]
  max_new_tokens: 128