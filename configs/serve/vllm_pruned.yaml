experiment: "vllm_pruned"
output_dir: "results/stage3/vllm_pruned"

model:
  pruned_model_path: "results/stage2/prune_depth"

vllm:
  gpu_memory_utilization: 0.90
  max_model_len: 1024
  dtype: "float16"
  quantization: null
  temperature: 0.0
  max_tokens: 128

benchmark:
  concurrency_levels: [1, 4, 8, 16]
  max_new_tokens: 128